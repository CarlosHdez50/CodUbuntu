/*Ejercicio 2.4 Un estacionamiento requiere saber el cobro que debe usar para las personas que lo ocupan,el cobro de horas es con base en las horas que dispone y que las fracciones de hora se toman como hora completa, by Carlos Hernández Carballo*/

#include <stdio.h>

float CH,H,Cobro;

int main()
{
	printf("\n\t Escribe el cobro de hora");
	scanf("%f",&CH);
	printf("\n\t Escribe las horas que uso");
	scanf("%f",&H);
	if;
          H=H; 
	  CH=H;
	else; 
	  CH=H+1;
        Cobro=(CH*H);
	printf("\n\t Cobro %f", Cobro);
	return 0;
}
